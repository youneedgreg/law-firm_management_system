.next
node_modules
coverage
package-lock.json

# Original wireframe export — kept verbatim as a design reference.
design/

# Generated by `npm run docs:erd` and compared byte for byte by
# `src/infra/sql/erd.test.ts`. Prettier pads the markdown tables, which would
# put the committed file permanently at odds with its own generator — so the
# generator's output is the format, and this is the one file the formatter does
# not own.
docs/erd.md
